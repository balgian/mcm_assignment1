\section{Exercise 5} \label{P5}
\subsection{Q5.1}
$^0_1 T = \begin{pmatrix}
        ? & ? & ? & ? \\
        ? & ? & ? & ? \\
        ? & ? & ? & ? \\
        ? & ? & ? & ?
    \end{pmatrix}$
    
\textit{[Comment] Same structure for the other matrices } 
